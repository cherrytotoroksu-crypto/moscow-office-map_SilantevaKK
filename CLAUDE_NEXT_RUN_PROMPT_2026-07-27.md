# Промпт для следующего захода Claude Code

Продолжай проект `moscow-office-map` с локального коммита `92263f580254d47f28587b65a39b7c978ca7c6b5`. Не выполняй `push`.

Сначала полностью прочитай:

- `QA_ROUND2_REVIEW_2026-07-27.md`
- `qa_round2_review_2026-07-27.json`
- `qa_handoff.json`
- `QA_ACCEPTANCE_2026-07-27.md`
- память проекта

Независимая приёмка Codex:

- QA-001 — `ACCEPTED`;
- QA-004 — `ACCEPTED`;
- QA-006 — `ACCEPTED`;
- QA-008 — `ACCEPTED_SCOPED`;
- QA-007 — `NEEDS_REWORK` и блокирует отправку;
- QA-012 — новая находка: 12 строк `classifier.html`, 57 устаревших геополей;
- QA-011 — нужен автоматический блокирующий тест согласованности.

Работай строго в порядке `QA-007 → QA-012 → QA-011`.

## QA-007

Точка `55.794811, 37.592464` не принимается: это результат геокодирования сомнительного адреса `Сущёвский Вал, 5` около Савёловской, тогда как официальный сайт «Страна Девелопмент» описывает проект «Офисы на Рижской» с прямым выходом из метро «Рижская», а актуальные объявления указывают `Рижская площадь, 3`.

Не геокодируй `Сущёвский Вал, 5` повторно. Найди первичный источник точного участка: карту/данные официального сайта продаж, строительное разрешение, кадастровый участок либо официальный материал города или РЖД. Раздели маркетинговый и строительный адреса. Координату ставь только после подтверждения участка; иначе используй `BLOCKED_BY_EVIDENCE`.

Проверь `id=260` в `buildings_202603.json`, `buildings_202606.json`, связанных лотах и `classifier.html`. После подтверждения синхронно исправь адрес, координаты и геополя.

## QA-012

Проверь координаты 11 сущностей/12 строк, перечисленных в `QA_ROUND2_REVIEW_2026-07-27.md`. Только для подтверждённых координат пересчитай геополя через `scripts/recompute_geo.py`. Не выполняй слепую массовую замену. Запиши для каждой строки старые и новые значения.

## QA-011

Добавь автоматический тест, который разбирает `RAW_DATA` из `classifier.html`, независимо пересчитывает шесть геополей по GeoJSON и завершается ошибкой при любом расхождении. Тест должен быть воспроизводимым и запускаться одной командой перед отправкой изменений.

После работы:

1. Разбери все JSON.
2. Запусти существующий аудит.
3. Запусти новый тест геополей — ожидается ноль расхождений.
4. Проверь связи здания `id=260` с лотами и кварталами.
5. Обнови handoff со статусами `FIXED_PENDING_REVIEW` или `BLOCKED_BY_EVIDENCE`.
6. Не выполняй `push`.
7. Локальный fixup/checkpoint-коммит допустим, но пометь его как ожидающий приёмки. Не переписывай и не объединяй историю до ответа Codex.

В отчёте укажи по каждому QA-ID изменённые файлы и поля, старые/новые значения, первичные источники, команды проверок и оставшиеся сомнения.
