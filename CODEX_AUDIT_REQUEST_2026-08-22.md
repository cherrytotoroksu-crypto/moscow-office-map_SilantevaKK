# Запрос на внешний аудит Codex, 2026-08-22

Формат по образцу `QA_COORDINATION.md` (статусы `OPEN → CLAIMED → FIXED →
RECHECKED`). Все пункты ниже — находки Claude Code за сессии 2026-08-18 —
2026-08-22, где веб-проверка упёрлась в предел (противоречащие источники,
объект не найден, или вопрос выходит за рамки правки одного поля). Полные
трассировки — в указанных `outputs/*.md`.

## Правила

Те же, что в `QA_COORDINATION.md`: не чинить по ближайшей точке при
расхождении имени/адреса/координат, не сливать сущности без минимум двух
независимых подтверждений.

`data/building_dates.json` — по умолчанию не менять никому. **Разовое
исключение 2026-08-22**: пользователь явно снял запрет для Claude Code на
одну задачу (поиск дат ввода/старта продаж/начала стройки для уже
известных пробелов) — см. `outputs/classifier_11_records_review_2026-08-19.md`,
раздел «Обновление 2026-08-22 (второй заход)». Запрет **не снят в общем
виде** — для любой другой правки файла нужно отдельное разрешение.

**Новое, 2026-08-22**: см. `CONSTRUCTION_STAGE_RETROSPECTIVE_SPEC_2026-08-22.md`
— план по историзации стадий стройки по кварталам (отдельная сущность
`stage_observation`, не замена `building_dates.json`). Реализация не
начата, но правки в `building_dates.json`/`all_projects_layer.json`
должны быть с ним совместимы — не перезаписывать даты без сохранения
предыдущего значения+источника, см. правила в самом spec-файле.

---

## AUDIT-001 — proj-168 «Orbital-2», GBA не совпадает ни с одним кандидатом

- Статус: `OPEN` (Codex, 2026-08-22, проверено без изменения данных;
  Claude Code, 2026-08-22, разобран исходный xlsx — тоже без изменения
  данных, см. ниже)
- Приоритет: высокий (похоже на ошибку сбора данных, не просто устаревшее поле)
- Запись: `data/all_projects_layer.json`, `canonical_project_id="proj-168"`,
  адрес «Москва, 3-я Магистральная ул., вл. 12», `gba=57967`, `gla=39667`,
  `project_status="Строится"`.
- Проверены и отклонены 3 кандидата (все не совпали по GBA):
  1. «Orbital» (без «-2»), 3-я Магистральная ул., **10**, Ultima
     Development, GBA ~27 271–27 081, уже построен 2023/2024 —
     `proj-101` в слое, отдельная запись, НЕ сливать.
  2. Здание на самом вл.12 — класс B, 17,9 тыс. кв. м, построено 1990.
  3. «Магистральная 12» (ФСК) — реальный адрес оказался «5-я
     Магистральная ул., вл. 4» (другая улица!), GBA 18 746.
- Ни один источник не называет проект «Orbital-2».
- **Разобран исходный `Классификатор_зданий.xlsx` (Claude Code,
  2026-08-22)** — это единственный локальный xlsx в репозитории (не в
  git, см. `.gitignore`), не в вебе:
  - Строка 169: `Orbital-2`, `id=168`, адрес «Москва, 3-я Магистральная
    ул., вл. 12», GBA 57967, GLA 39667, девелопер Ultima Development,
    статус «Строится» — значения **точно совпадают** с
    `all_projects_layer.json`, никакой ошибки конвертации xlsx→JSON нет.
  - Строка 102: `Orbital` (без «-2»), `id=101`, тот же девелопер, адрес
    вл. **10**, GBA 27271 — соответствует уже проверенному `proj-101`.
  - Соседние строки 167/168/170 (SOK Арена Парк/Земляной Вал/Сады
    Пекина) — другой девелопер, GBA пустой — GBA 57967 **не могло**
    попасть в строку Orbital-2 копипастом соседней ячейки.
  - `Первое появление`/`Последнее появление` для Orbital-2: Q2 2025 →
    Q1 2026 (4 квартала подряд, тот же GBA каждый раз) — запись не
    разовый глюк одного сбора, кто-то стабильно фиксировал именно эти
    цифры на протяжении года. Для Orbital (без «-2») трек начинается
    раньше — Q2 2023, что согласуется с гипотезой «второй, более
    поздний, менее публичный этап того же девелопера», а не с ошибкой
    дублирования первого этапа.
  - **Вывод**: это не баг конвейера сбора/конвертации данных — сама
    исходная таблица (составитель `Классификатор_зданий.xlsx`, не
    видно кем и на основе чего) независимо и стабильно называет
    «Orbital-2» отдельным объектом с этими цифрами. Не могу определить,
    откуда составитель таблицы взял эти данные (нет доступа к его
    первичным источникам) — публичного веб-подтверждения по-прежнему
    нет ни для здания, ни для его GBA. Остаётся `OPEN`: не ошибка
    JSON-пайплайна, но и не подтверждено веб-поиском — вопрос к
    первоисточнику таблицы, а не к коду.
- Полная трассировка: `outputs/classifier_11_records_review_2026-08-19.md`,
  раздел `proj-168`.
- Просьба к Codex: разобрать исходный `classifier.html`/RAW_DATA — откуда
  взялись GBA 57967 и суффикс «-2» для этой строки; проверить, не спутана
  ли запись с другим объектом на этапе исходного сбора таблицы.

## AUDIT-002 — proj-227 «БЦ Сколковский», объект не найден вообще

- Статус: `OPEN` — перепроверено Codex 2026-08-24; активная перепроверка
  первичного реестра и точной координаты не дала данных для безопасной
  правки `developer`/координат/дедупликации недостаточно
- Приоритет: высокий
- Запись: адрес «Кулибина ул., д. 8 стр. 1», `developer="Пожарная охрана"`.
- Ни по адресу, ни по названию не нашлось ни одного источника (офиц.
  сайт, ЦИАН, 2ГИС, агрегаторы). `developer="Пожарная охрана"` похоже на
  артефакт парсинга (перепутанная колонка), не на реальное имя компании.
- Полная трассировка: `outputs/classifier_11_records_review_2026-08-19.md`,
  раздел `proj-227`.
- Просьба к Codex: сверить со строкой в исходном `classifier.html` —
  какая колонка реально должна быть `developer`, существует ли объект под
  другим именем/адресом рядом с координатами 55.693668, 37.349749.
- Проверка Codex 2026-08-22:
  - смещения колонок при сборке `classifier.html` **нет**: в исходном
    `Классификатор_зданий.xlsx`, лист `Классификатор`, строка 228,
    значение «Пожарная охрана» уже находится именно в колонке O
    `Девелопер`; те же поля повторяются в слоях `buildings` обоих GPKG.
    Значит, аномалия пришла из исходных данных, а не из JSON-парсера;
  - объект не вымышлен: официальный реестр mos.ru называет по точному
    адресу «территория инновационного центра Сколково, ул. Кулибина,
    д. 8 стр. 1» объект `ИЦ "Сколково" ХЦ № 2` и эксплуатанта ООО
    «АЭРОПРОФ»; независимая публикация по исследованию NF Group называет
    БЦ «Сколковский» площадью около 8,5 тыс. м², реализуемый целиком.
    Проверено 2026-08-22: `https://www.mos.ru/upload/documents/files/252/PrilojeniereestrNVOS.pdf`,
    `https://logistics.ru/upravlenie-logistikoy-i-kompaniey-upravlenie-operaciyami/odnogo-metro-nedostatochno-chto-opredelit`;
  - квартальные `data/lots_202410.json` — `lots_202606.json` независимо
    от RAW_DATA подтверждают непрерывное предложение всего здания
    площадью 8500 м², но не содержат девелопера;
  - подставлять «АЭРОПРОФ» в `developer` нельзя: официальный реестр
    подтверждает эксплуатанта экологически значимого объекта, а не
    девелопера. Занулять «Пожарную охрану» тоже не стал без provenance
    четырёх исходных листов, где это значение уже повторяется;
  - координаты не менялись: рядом существуют расходящиеся точки в
    источниках, а правило запрещает чинить по ближайшей. С `proj-22`
    (одно исходное имя/GBA, но адрес Большой бульвар, 40) запись не
    объединялась: официальный сайт Сколково называет по этому адресу
    БЦ «Амальтея», однако прямой источник, связывающий `proj-22` и
    `proj-227` как одну сущность, не найден;
  - `data/all_projects_layer.json`, квартальные `data/*.json` и
    `data/building_dates.json` не менялись. Для продолжения нужен
    первичный источник четырёх листов (`будущие`, `будущие (old)`,
    `ОСНОВНАЯ`, `в рынке`) либо карточка продавца/правообладателя с
    явным полем девелопера.
  - Открытый вопрос (не решён, нужен для закрытия AUDIT-002): что в
    действительности означает значение «Пожарная охрана» в исходной
    колонке `Девелопер` — реальная компания-девелопер под этим названием,
    указание на назначение/эксплуатанта объекта (пожарная часть/охрана
    объекта), потенциальный покупатель, или поле, ошибочно перенесённое
    из другой колонки исходной таблицы (например, из описания/арендатора)
    ещё до попадания в `Классификатор_зданий.xlsx`. Без ответа на этот
    вопрос менять `developer` нельзя — ни на «АЭРОПРОФ», ни на null.

- Перепроверка 2026-08-24: официальный реестр `mos.ru` снова подтверждает
  точный адрес и эксплуатанта ООО «АЭРОПРОФ»; поиск по точному адресу,
  названию «БЦ Сколковский» и карточкам ФСК не дал источника, который
  связывает этот корпус с девелопером или подтверждает новую координату.
  Найдены только независимое описание NF Group (около 8,5 тыс. м²) и
  официальный реестр; это не закрывает вопрос developer/coordinates.
  Данные не менялись, пункт оставлен `OPEN`.

## AUDIT-003 — proj-200 «STONE Ходынка III», конфликт даты ввода

- Статус: `RECHECKED` (Claude Code, 2026-08-22, второй заход — автопроверка,
  не независимая проверка Codex)
- Приоритет: средний
- Было: `input_year=null`, конфликт «2028» (stone.ru, поисковый сниппет)
  vs «2029» (stonebrokers.ru).
- Разобрано: конфликт был между поисковым СНИППЕТОМ stone.ru (заголовок
  страницы в выдаче — устаревшая SEO-метаданная) и РЕАЛЬНЫМ содержимым
  той же страницы. Прямой WebFetch (не поиск) stone.ru/commercial/hodinka3
  даёт «Готовность 2029» — не 2028. stonebrokers.ru и
  kommersant.ru/doc/8228995 (статья о получении разрешения на
  строительство) независимо подтверждают тот же 2029.
- Применено: `input_year=2029`, `input_quarter=null` (квартал не
  уточнён), `input_date_kind=confirmed`. `data/building_dates.json`
  дополнен записью `"stone ходынка iii"`.
- Полная трассировка: `outputs/classifier_11_records_review_2026-08-19.md`,
  раздел `proj-200` (обновлён 2026-08-22, второй заход).
- Regression: `tests/test_classifier_11_records_review.py::test_stone_khodynka_3_completion_year_conflict_resolved`.
- Осталось для Codex: ничего обязательного — при желании можно
  независимо перепроверить, что 2029 (не 2028/другое значение) —
  честная проверка снимка страницы полезна, но не блокирует.

## AUDIT-004 — proj-1 «STONE Пресня», не подтверждён

- Статус: `CONFLICT/BLOCKED` (существование подтверждено Wave 01 Codex
  2026-08-22, дата ввода — НЕТ, конфликт 2026-08-22, не закрывать)
- Приоритет: средний
- Адрес «Ходынская ул., вл. 2, стр. 9» не совпадает ни с одной официальной
  страницей STONE (проекты на Ходынском поле — пронумерованные «Ходынка
  1–4» на Ходынском бульваре). Единственное совпадение — агрегатор
  rosretail.com/stone-presnya, недостаточно для подтверждения.
- **Обновление (Wave 01, Codex, 2026-08-22): существование подтверждено.**
  ibcrealestate.ru/catalog/T96_13282 и of.ru/bc/12524 независимо
  называют тот же точный адрес, GBA 16277 м² совпадает у обоих. GBA/GLA
  (16277/13338) применены в `data/all_projects_layer.json`.
- **Обновление (внешняя перепроверка, 2026-08-22): дата ввода —
  КОНФЛИКТ, НЕ подтверждена, не закрывать.**
  - `bc-stone-presnya.ru` — «2024 год постройки»;
  - `of.ru/bc/12524` — «2027 год постройки», **без квартала** (Wave 01
    прочитал это как подтверждение Q1 2027 вместе с IBC Real Estate —
    при повторной проверке of.ru квартал не указан явно, только год);
  - публикация Region-Invest (2021) — только стадия планирования, без
    срока ввода.
  - Три источника трёх разных лет/уровней точности (2024 факт vs 2027
    план vs 2021 без даты) — `commission_q="202703"` в
    `data/building_dates.json` (ключ `"stone пресня"`) остаётся как
    **low-confidence/planned**, не как подтверждённый факт. Q1 2027
    **не считать подтверждённым**. Менять дату нельзя без официального
    источника STONE (не агрегатора) или Мосгосстройнадзора.
  - Эта правка **не внесена в данные** этим коммитом (только
    зафиксирована здесь) — см. ограничение «Техническую часть не
    менять» в сообщении пользователя 2026-08-22: `data/building_dates.json`
    и `data/all_projects_layer.json` не тронуты, статус
    `verification_status=under_review`/`commission_q=202703` остаются
    как есть, только эта запись предупреждает не считать их
    подтверждёнными.
- Полная трассировка: `outputs/classifier_11_records_review_2026-08-19.md`,
  раздел `proj-1`.
- Просьба к Codex: установить, существует ли проект «STONE Пресня» как
  отдельный официальный объект, либо это устаревшее/внутреннее название
  одного из «Ходынка 1–4».

## AUDIT-005 — Multispace Павелецкая, адрес скопирован с другой точки сети

- Статус: `RECHECKED` (Claude Code, 2026-08-22 — регрессионным тестом
  `tests/test_multispace_pavel_address_fix.py`, 207/207 полного набора
  зелёные; это автопроверка, не независимая проверка Codex)
- Приоритет: средний (подозрение на системную проблему в исходных данных)
- Было: `data/coworking_202512.json`, `coworking_202603.json`,
  `coworking_202606.json` — запись `name="Multispace Павелецкая"`,
  `bc="1 шипковский пер. 5"`, поле `address` указывало «Ленинградский
  проспект,36,корп. 8» — адрес ДРУГОЙ точки той же сети («Multispace
  Динамо», ВТБ Арена). В Q1 2026 (`coworking_202603.json`) координаты тоже
  были скопированы с Динамо (55.791137, 37.559353).
- Правка: `address` во всех трёх файлах → **«1-й Щипковский переулок, 5»**.
  Координаты Q4 2025 (`202512`, 55.722593/37.632035) и Q2 2026 (`202606`,
  те же) уже были верны — не менялись. Q1 2026 (`202603`) координаты
  исправлены на 55.722593/37.632035 (точное совпадение адреса, источник
  ниже), старое значение сохранено в новых полях `prev_lat`/`prev_lng`
  (по образцу `geocoding_fix_source` из фикса Know Where в этом же файле).
  В каждую из 3 записей добавлено поле `address_fix_source` с датой
  проверки, ссылкой на источник и ссылкой на этот AUDIT-005.
- Источник координат: yandex.ru/maps —
  https://yandex.ru/maps/213/moscow/house/1_y_shchipkovskiy_pereulok_5/37.631990,55.722532
  — проверено 2026-08-22.
- Multispace Павелецкая и Multispace Динамо остались раздельными записями
  (разные `id`, разные адреса/координаты) — не объединялись.
- Regression: `tests/test_multispace_pavel_address_fix.py` (6 тестов) —
  пинит адрес/координаты по всем 3 кварталам, что Q1 2026 хранит прежнее
  (ошибочное) значение в `prev_lat/prev_lng`, наличие source+даты, и что
  Павелецкая и Динамо не совпадают ни по адресу/координатам, ни по `id`.
- `data/building_dates.json` не менялся.
- Полная трассировка: `outputs/coworking_missing_class_qa_2026-08-19.md`,
  раздел «Пятая (финальная) партия».
- Осталось для Codex: проверить весь `data/coworking_*.json` на другие
  подобные копипаст-ошибки адреса внутри одной сети — этот один случай
  исправлен точечно, системную проверку по всем файлам не проводил.
- **Системная проверка выполнена (внешняя перепроверка, 2026-08-22):**
  автоматический аудит всех `data/coworking_*.json` на повторные
  копипаст-адреса (как у Multispace Павелецкая/Динамо) и на совпадающие
  координаты у разных ID — **ничего не найдено**, других случаев того же
  типа в файлах коворкингов нет. Итог зафиксирован здесь; отдельного
  outputs-файла с построчной трассировкой этого скана не приложено —
  если нужна воспроизводимая трассировка, запросить у источника
  проверки скрипт/лог, использованный для скана.

## AUDIT-006 — 9 coworking-записей, класс здания расходится по источникам

- Статус: `OPEN` (Codex, 2026-08-22; проверено, исправление не применено
  из-за неоднозначности первичной записи)
- Приоритет: низкий (не блокирует ничего, справочное поле)
- Список 9 записей и по каждой — какие источники и какие классы
  расходятся — в `outputs/coworking_missing_class_qa_2026-08-19.md`,
  финальный раздел «Оставшиеся 9».
- Просьба к Codex: если есть доступ к более авторитетному единому
  классификатору (например, внутренняя база NF/CRE) — свериться напрямую,
  вместо перебора публичных агрегаторов.
- Проверено 2026-08-22: по Space 1 Арбат (Арбат, 10) три независимых
  рыночных источника — [IBC](https://ibcrealestate.ru/catalog/T96_12118/),
  [ICX](https://icx.ru/bc/midland-plaza/) и [ЦИАН](https://www.cian.ru/rent/commercial/328174553/)
  — классифицируют здание как B+. Однако в текущем слое это коворкинг-хост
  с `cls=null`, а историческая запись не присутствует в актуальных
  `coworking_*.json`; безопасной строки для исправления нет. Остальные
  восемь записей требуют адресной проверки по конкретному корпусу — общий
  класс комплекса подставлять нельзя. Данные не менялись.

## AUDIT-010 — proj-240 и proj-258, класс расходится с офиц. сайтом застройщика

- Статус: `FIXED` (Codex, 2026-08-22; независимая перепроверка классов)
- Приоритет: низкий (справочное поле, не блокирует данные)
- `proj-240` «Ольховая SKY»: у нас `cls="B+"`, fortexgroup.ru/bc/olhovaya-sky
  указывает класс **B**.
- `proj-258` «STONE Ходынка IV»: у нас `cls="A"`, stone.ru/commercial/hodinka4
  указывает класс **Prime** (как у соседней Ходынка III, где Prime уже
  подтверждён и совпадает).
- Оба расхождения зафиксированы, НЕ исправлены — это не coworking-записи
  (отдельно от AUDIT-006), офисные проекты с собственным `project_status`.
- Исправлено 2026-08-22: `proj-240.cls` B (Fortex Group, IPG.Estate, CIAN,
  CREMAP согласуются); `proj-258.cls` Prime (официальная страница STONE и
  STONE Brokers используют «премиальный», CREMAP и Yandex Realty — A;
  каноническая нормализация STONE «премиальный» → Prime).
- Источники и дата проверки добавлены в `qa_notes` обеих записей.
- Regression-тесты добавлены/обновлены в
  `tests/test_classifier_11_records_review.py`; полный набор: 269/269.
- Полная трассировка: `outputs/classifier_11_records_review_2026-08-19.md`,
  разделы `proj-240` и `proj-258`.
- Просьба к Codex: подтвердить класс по второму независимому источнику
  (не только сайт застройщика — застройщики иногда указывают маркетинговый
  класс выше реального) и поправить `cls`, если расхождение реальное, а
  не просто иначе классифицированное здание.

## AUDIT-007 — proj-33 «STONE Towers», риск задвоения GBA/уникальных проектов

- Статус: `OPEN` (Codex, 2026-08-22; проверено, безопасная дедупликация
  не подтверждена)
- Приоритет: средний (искажает агрегаты кодификатора, не блокирует данные)
- `proj-33` («STONE Towers», GBA 85262, `offer_status="Ещё не вышел в
  продажу"`) сосуществует как отдельная запись рядом со своими же тремя
  башнями — `proj-88` (Tower A, GBA 13900, «Продано / снято»), `proj-89`
  (Tower D, GBA 47000), `proj-90` (Towers BC, GBA 65500). Сумма башен
  126400 ≠ GBA `proj-33`.
- Проверено построчным поиском по всем `data/lots_2*.json`/
  `data/rent_lots_2*.json` (2021 Q2 – 2025 Q1): ключ `"STONE Towers"` —
  только флэт, `"Tower A"`/`"Tower D"`/`"Towers BC"` не встречаются НИ
  РАЗУ ни в одном квартальном файле. `proj-33`, похоже, пришёл из
  квартального пайплайна (устаревший, ещё до разбивки на башни),
  `proj-88/89/90` — добавлены отдельно, более точные пообъектные данные.
  `proj-33` не помечен `duplicate_of`, `qa_notes` без перекрёстной ссылки.
- Полная трассировка: `outputs/project_building_lot_audit_2026-08-22.md`,
  раздел «STONE — найдена структурная проблема».
- Просьба к Codex: подтвердить/опровергнуть, что `proj-33` — legacy-дубль
  суммы трёх башен (не отдельная непроданная часть комплекса), и если да
  — предложить, как пометить (duplicate_of на какую из трёх? отдельный
  канонический «родитель-комплекс»?), не удаляя запись.
- Проверено 2026-08-22: официальный STONE указывает, что STONE Towers —
  единый квартал из четырёх башен общей площадью 106 тыс. кв. м
  ([STONE](https://stone.ru/presscenter/stone-towers-pobeditel-premii-best-office-awards-2025);
  [презентация STONE](https://stone-s3.storage.yandexcloud.net/STONE_Office_1156d0b659.pdf)).
  Независимые карточки подтверждают отдельные башни A (14 тыс.), B/C и D
  (40 тыс.); [CRE](https://cre.ru/news/95530) также приводит 40 тыс. для D.
  Это подтверждает иерархию «квартал → башни», но не доказывает, что
  `proj-33` (GBA 85 262) — именно сумма/legacy-дубль башен `proj-88/89/90`:
  площади не совпадают с опубликованными 106 тыс., а отдельный канонический
  parent-объект отсутствует. Ничего не объединял и не удалял; требуется
  первичный реестр площадей/корпусов STONE.

## AUDIT-008 — Manufaqtury Poklonka Place, решение об объединении не применено

- Статус: `FIXED` (Codex, 2026-08-22)
- Приоритет: низкий
- `proj-83` (55.737258, 37.534514), `proj-84`/`proj-174` (оба
  55.736179, 37.533194, ~160 м от `proj-83`). Решение
  `outputs/unified_codifier_review_decisions_2026-08-18.md` («Уточнение…»):
  «Объединить проект, но сохранить минимум два building-кандидата; не
  схлопывать точки» — принято, но ни у одной из трёх записей `qa_notes`/
  `duplicate_of` не отражает это решение.
- Полная трассировка: `outputs/project_building_lot_audit_2026-08-22.md`,
  раздел «Manufaqtury».
- Просьба к Codex: применить решение (пометить связь между 3 записями
  building-кандидатами, не сливая координаты) или явно задокументировать,
  почему решение не применяется.
- Применено: `data/all_projects_layer.json` — `proj-84` и `proj-174`
  помечены `duplicate_of="proj-83"`, у `proj-83` заполнен `legacy_ids`;
  все три строки и обе исходные координатные точки сохранены.
- Устойчивость при регенерации: связь добавлена в
  `scripts/build_all_projects_layer.py::DUPLICATE_GROUPS` с отдельной
  пометкой structured-группы, чтобы не применять к ней правило
  «одинаковая координата» обычных технических дублей.
- Независимые подтверждения, проверены 2026-08-22 и записаны в `qa_notes`:
  `https://cntez.ru/manufaqtury` (Manufaqtury, БЦ Poklonka Place,
  Поклонная ул., 3) и `https://poklonka-place.com/` (единый комплекс по
  адресу Поклонная ул., 3, несколько башен/корпусов).
- Regression: `tests/test_manufaqtury_poklonka_audit_008.py`; обновлён
  общий инвариант в `tests/test_all_projects_layer_registry.py`.
  Полный набор: 221/221 тест зелёный; `git diff --check` — без ошибок.
- `data/building_dates.json` не менялся.

## AUDIT-009 — Speace Jet Красная Роза, дедуп не завершён

- Статус: `OPEN` (Codex, 2026-08-22; проверено, правка отложена из-за
  недостаточного подтверждения идентичности корпуса)
- Приоритет: низкий
- 3 из 4 площадок Speace Jet задублированы через `duplicate_of`
  (Белая площадь `proj-65→28`, Аркус 3 `proj-131→29`, Якиманка
  `proj-133→30`). 4-я — Красная Роза (`proj-27` 55.734233,37.587721 /
  `proj-76` 55.735241,37.587623, ~112 м) — не помечена, хотя решение
  2026-08-18 её тоже касалось (мягче: «сохранить как разные входы/
  корпуса до подтверждения адреса»).
- Полная трассировка: `outputs/project_building_lot_audit_2026-08-22.md`,
  раздел «Speace Jet».
- Просьба к Codex: подтвердить адрес корпуса и завершить разметку (либо
  `duplicate_of`, либо явные building-метки, как решение и предполагало).
- Проверено 2026-08-22: материалы Space 1 JET и CIAN указывают адрес
  **ул. Тимура Фрунзе, 11с13**; [Яндекс Карты](https://yandex.com/maps/213/moscow/house/ulitsa_timura_frunze_11s13/Z04Ycw9oSkYFQFtvfXtydXtgYA==/)
  дают точку 55.734735, 37.588332. Обе записи классификатора содержат
  11с33 и разные точки. Это сигнал ошибки адреса/геокодинга, но источники
  не доказывают, что строки 11с33 и 11с13 — один и тот же корпус или что
  обе записи можно безопасно схлопнуть. Адреса, координаты и `duplicate_of`
  не менялись; требуется первичный реестр корпусов или подтверждение
  оператора для закрытия тикета.

## AUDIT-011 — Полнота общей карты и географическая верификация всех проектов

- Статус: `CLAIMED` (Codex, 2026-08-22) — начата последовательная волновая
  верификация всех оставшихся площадей и дат по независимым источникам. Выполнен сквозной building-level
  матчинг каналов sale/rent/coworking, заполнение подтверждаемых дат и площадей,
  полный regression, commit и push. До завершения массовой правки не считать
  текущий слой финальным. Ранее пользователь подтвердил, что
  `coworking_site` и `host_building` остаются разными публичными сущностями с
  разными обязательными полями. Решение применено, но обязательные данные
  реальных офисных проектов/зданий заполнены ещё не полностью.
- Приоритет: высокий
- Объём: сверить `data/all_projects_layer.json` со всеми историческими
  `data/buildings_*.json`, `data/lots_*.json`, `data/rent_lots_*.json` и
  `data/coworking_*.json`; проверить покрытие каналов sale/rent/coworking,
  координаты и выбросы относительно Москвы, GBA/GLA и даты жизненного цикла.
- Правило заполнения: не подставлять догадки или нули вместо неизвестных
  значений. Подтверждённая правка получает источник и дату в `qa_notes`;
  неподтверждённый пробел остаётся явно отмеченным в отчёте проверки.
- Ограничения: `data/building_dates.json` изменён после отдельного разрешения
  пользователя 2026-08-22 («все даты»); сущности не объединять без
  двух независимых подтверждений; координаты не исправлять по ближайшей точке.
- Воспроизводимая трассировка:
  `outputs/all_projects_completeness_audit_2026-08-22.md/.json` и
  `scripts/audit_all_projects_completeness.py`.
- Исправлено: 61 публичная запись `coworking_host_lookup` получила координаты
  из `data/coworking_202606.json`; источник и дата добавлены в `qa_notes`.
  Для Балчуга выбран дом 9 по точному адресу (`id=233`), не ближайшая точка.
  После правки публичных канонических записей без координат: 0; географических
  выбросов за guardrail Москвы: 0.
- Ролевая модель: в 374 строках задан `entity_role`; среди 338 публичных
  канонических строк — 129 `office_project`, 116 `coworking_site`, 93
  `host_building`. 32 исторических host восстановлены; 133/147 московских
  site-записей связаны с хостом, 14 неоднозначных оставлены internal_only.
  Правила закреплены в
  `scripts/all_projects_entity_roles.py`, схеме, генераторах и спецификации.
  Источник решения и дата `2026-08-22` записаны в `qa_notes` каждой строки.
- Осталось по применимому знаменателю 222 офисных проектов/зданий: GBA
  заполнена 129/222 (58,1%; отсутствует 93), GLA 114/222 (51,4%; отсутствует
  108), начало строительства 34/222, год ввода 87/222. Для 116
  `coworking_site` эти поля неприменимы и не копируются из здания-хоста.
  110+ неоднозначных identity-групп не объединялись. Начало стройки/продаж
  добавлено в схему; `stage_observation` для квартальной ретроспективы не реализован.
- Волна 01 (Codex, 2026-08-22): по двум независимым источникам на точный адрес
  исправлены площади STONE Пресня (GBA 16 277, GLA 13 338 м²) и Plaza
  Technopark (GBA 32 138, GLA 23 358 м²). GLA-покрытие стало 114/222
  (51,4%; отсутствует 108). Трассировка:
  `data/qa/codifier_gap_wave_01_20260822.json`, миграция
  `scripts/apply_codifier_gap_wave_01.py`, regression
  `tests/test_codifier_gap_wave_01.py`. Найденные даты STONE Пресня, Plaza
  Technopark и PORTA не применены: среда потребовала отдельного явного
  разрешения именно на `data/building_dates.json`.
- Regression: `tests/test_all_projects_completeness_audit.py`,
  `tests/test_all_projects_layer_registry.py`,
  `tests/test_validate_all_projects_layer.py`.
- `data/building_dates.json` теперь содержит canonical ID для всех 93 записей.
  Статус не повышен до `FIXED`, потому
  что неподтверждённые площади/даты нельзя заполнять догадками, а перенос GBA/
  GLA здания-хоста в operator-site создаст двойной учёт.
- Волна 02 (Claude Code, 2026-08-22): применены даты по прямому указанию
  пользователя — **STONE Пресня** `commission_q=202703` (Q1 2027, план,
  IBC Real Estate + Of.ru, 2 независимых источника); **PORTA**
  `commission_q=202509` (Q3 2025) — применено с **низкой уверенностью и
  зафиксированным конфликтом**: независимая перепроверка (2ГИС,
  2026-08-22) НЕ подтвердила заявленный пользователем работающий
  Multispace в здании и сам 2ГИС всё ещё называет объект «строящийся»;
  единственный найденный источник даты (cre.ru/news/95248) — это
  плановый срок девелопера при ~50% готовности, не подтверждённый факт
  ввода. **Plaza Technopark** не менялся — уже был корректен
  (`commission_q=202112`, подтверждён 2026-07-16). `input_year`/
  `input_quarter` синхронизированы в `data/all_projects_layer.json` через
  `scripts/migrate_all_projects_dates.py` (НЕ через
  `build_all_projects_layer.py` — тот перегенерирует файл с нуля и
  стирает все внешние слои: `coworking_host_lookup`, `remain_datalens`,
  `entity_role` и т.д.; это было по ошибке запущено и сразу отменено
  через `git checkout`, слой не пострадал в итоговом коммите).
  Трассировка: `data/qa/codifier_gap_wave_02_dates_20260822.json`,
  миграция `scripts/apply_codifier_gap_wave_02_dates.py`, regression
  `tests/test_codifier_gap_wave_02_dates.py`. PORTA требует отдельной
  проверки реестра Мосгосстройнадзора по адресу Заречная ул., вл. 2/1,
  прежде чем повышать уверенность.
- Дальнейшие волны (GBA/GLA для 93 office_project без площади и т.д.) не
  начаты в эту сессию — объём слишком велик для одного захода при текущем
  бюджете; сессия заканчивается на волне 02, продолжение — по запросу.
- Независимый recheck волны 03 (Codex, 2026-08-24; backlog-трек, не точечные
  статусы): проверены `proj-95`, `proj-102`, `proj-103`. У `proj-103`
  подтверждена GLA 48 000 м² двумя независимыми рыночными отчётами (NF Group
  Research и IBC Real Estate/JLL) и применена с источниками/датой в
  `qa_notes`. `proj-95` и `proj-102` оставлены `null`: отдельные площади
  публикуются, но значения конфликтуют. Трассировка:
  `data/qa/codifier_gap_wave_03_gla_recheck_20260824.json`, миграция
  `scripts/apply_codifier_gap_wave_03_gla_recheck.py`, regression
  `tests/test_codifier_gap_wave_03_gla_recheck.py`.
- Синхронизация live-копии (Codex, 2026-08-24): сохранено подтверждение
  River Park Q2 2026; конфликт Chalet нормализован в пользу актуального
  официального Q2 2028, более ранние 2027 оставлены только как история;
  неподтверждённая замена адреса/координат `OBJ-0196` не принята. Начальный
  `data/unified_classifier.json` сохраняет все 705 строк исходного реестра,
  включая 25 строк без координат, и использует только консервативные
  exact-name/exact-address связи; 589 строк явно оставлены `needs_review`.
  Сквозные `unified_id` закреплены за стабильными `source_id`, а не за
  позицией строки. Готово к `RECHECKED`: полный unittest — 294 теста `OK`,
  `validate_all_projects_layer.py` — `PASS` для 374 строк, повторный build
  кодификатора — 705 строк (680 с координатами, 25 явно без координат).

---

## Как отвечать

По образцу `QA_COORDINATION.md`: сменить статус на `CLAIMED` перед началом
работы, `FIXED` — после правки с указанием файла/строки/источника,
`RECHECKED` — после независимой машинной проверки. Не редактировать
`data/all_projects_layer.json` / `data/coworking_202606.json` напрямую в
процессе активной сессии Claude — сначала синхронизация здесь.
