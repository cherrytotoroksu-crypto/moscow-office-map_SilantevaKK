Проверь коммиты d2accc9..f2dd67b в moscow-office-map (AUDIT-011, волна 03, Claude Code, 2026-08-23) плюс синхронизируй со своей текущей рабочей копией.

Что сделано в волне 03 (docs-only, данные НЕ менялись):
Батч из 11 office_project с заполненным GBA, но gla=null (proj-13/33/57/95/98/102/103/104/275/276, plus proj-227 пропущен — отдельный AUDIT-002). Проверено 10, GLA заполнено 0. Причины по каждому — либо источник называет "общую" и "офисную" площадь ОДНИМ числом (=нашему GBA, отдельного GLA не существует в природе), либо источник описывает другое зерно (весь МФК/квартал вместо конкретного корпуса нашей записи — Парк Легенд класс А/В+, Королев/Рассвет). Полный список с источниками — outputs/missing_data_backlog_2026-08-22.md, разделы "Обновление 2026-08-23 — Claude Code, волна 03".

Просьба к тебе:
1. Перепроверить 2-3 случайных из десяти (например Алкон 3 и Парк Легенд класс А) — подтвердить, что GLA действительно не публикуется отдельно, а не что я плохо искал.
2. Если у тебя есть доступ к источникам, которые я не проверял (платные CRE-базы, внутренние выгрузки NF Group) — попробовать закрыть хотя бы часть этих 10 записей настоящими числами.
3. Важно: НЕ путать этот batch (свободный поиск по gla=null, докс-трек missing_data_backlog) с точечными открытыми находками в CODEX_AUDIT_REQUEST_2026-08-22.md (AUDIT-001..011) — это разные треки, статусы не смешивать.

Отдельно — синхронизация состояния:
На момент коммита f2dd67b у меня в рабочей копии (НЕ закоммичено, не мои изменения) видны твои live-правки поверх git:
- изменены: data/all_projects_layer.json, data/building_dates.json, data/future_projects_verification_overrides.json
- новые непрокоммиченные: data/unified_classifier.json, scripts/build_unified_classifier.py, tests/test_unified_classifier.py

Я их не трогал и не коммитил (не мои). Когда закоммитишь свою часть — сделай `python -m unittest discover -s tests -p "test_*.py"` и `python scripts/validate_all_projects_layer.py data/all_projects_layer.json` перед push, дай знать в CODEX_AUDIT_REQUEST_2026-08-22.md (AUDIT-011) что именно готово к RECHECKED.
